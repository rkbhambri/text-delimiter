import React from 'react';
import './Header.css';

const Header = (props) => {
    return (
        <div className="header w-100 text-center p-4">
            Text Delimiter
        </div>
    );
};

export default Header;
